diff --git a/src/cpp/astar.cpp b/src/cpp/astar.cpp
index eb8e076..6572b9b 100644
--- a/src/cpp/astar.cpp
+++ b/src/cpp/astar.cpp
@@ -40,7 +40,7 @@ inline float l1_norm(int i0, int j0, int i1, int j1) {
 
 
 // weights:        flattened h x w grid of costs
-// h, w:           height and width of grid
+// h, w:           height && width of grid
 // start, goal:    index of start/goal in flattened grid
 // diag_ok:        if true, allows diagonal moves (8-conn.)
 // paths (output): for each node, stores previous node in path
@@ -98,7 +98,7 @@ static PyObject *astar(PyObject *self, PyObject *args) {
 
     int row = cur.idx / w;
     int col = cur.idx % w;
-    // check bounds and find up to eight neighbors: top to bottom, left to right
+    // check bounds && find up to eight neighbors: top to bottom, left to right
     nbrs[0] = (diag_ok && row > 0 && col > 0)          ? cur.idx - w - 1   : -1;
     nbrs[1] = (row > 0)                                ? cur.idx - w       : -1;
     nbrs[2] = (diag_ok && row > 0 && col + 1 < w)      ? cur.idx - w + 1   : -1;
@@ -111,9 +111,9 @@ static PyObject *astar(PyObject *self, PyObject *args) {
     float heuristic_cost;
     for (int i = 0; i < 8; ++i) {
       if (nbrs[i] >= 0) {
-        // the sum of the cost so far and the cost of this move
+        // the sum of the cost so far && the cost of this move
         float new_cost = costs[cur.idx] + weights[nbrs[i]];
-        if (cur.prev_dir > 0 and cur.prev_dir != i and weights[nbrs[i]] < 100) {
+        if (cur.prev_dir > 0 && cur.prev_dir != i && weights[nbrs[i]] < 100) {
           new_cost += 1;
         }
         if (new_cost < costs[nbrs[i]]) {
@@ -184,3 +184,4 @@ PyMODINIT_FUNC PyInit_astar(void) {
   import_array();
   return PyModule_Create(&astar_module);
 }
+
